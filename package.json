{
  "name": "appointment-platform",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "install-all": "npm install --legacy-peer-deps ; npm install concurrently --save-dev",
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
